input {

    udp {
        port => "9999"
        codec => json
    }

} filter {

    geoip {
        source => "ip"
    }

} output {

    elasticsearch {
        hosts => ["localhost:9200"]
        index => test_index
    }

}
